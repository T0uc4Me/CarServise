<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=ABeeZee:ital@0;1&family=Krona+One&family=Unbounded:wght@200..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/5style.css">
    <title>Восстановление пароля | Car Service</title>
</head>
<body>
    <header class="page-header">
      <a href="/"><img src="/img/svg/Logo.svg" alt="logo" class="Logo"></a>
    </header>

    <main class="container">
      <h2>Восстановление пароля</h2>

      <% if (error) { %>
          <div class="feedback error"><%= error %></div>
      <% } %>
  
      <% if (success) { %>
          <div class="feedback success"><%= success %></div>
      <% } %>

      <form action="/auth/reset-password" method="POST">
          <label for="new-password">Введите новый пароль</label>
          <input type="password" id="new-password" name="newPassword" required>

          <label for="confirm-password">Повторите пароль</label>
          <input type="password" id="confirm-password" name="confirmPassword" required>

          <div class="knopki">
              <a href="/auth/login" class="ghost-button">Назад</a>
              <button type="submit" id="next-button">Сохранить</button>
          </div>
      </form>
    </main>

    <script src="a5.js"></script>
</body>
</html>
