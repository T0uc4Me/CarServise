<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ошибка | Car Service</title>
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 20px;
            font-family: "Unbounded", Arial, sans-serif;
            color: #fff;
            background: radial-gradient(circle at top, #2a2743 0%, #0f0e17 60%);
        }

        .error-container {
            max-width: 780px;
            width: 100%;
            border-radius: 22px;
            padding: 28px;
            background: rgba(255, 255, 255, 0.06);
            border: 1px solid rgba(255, 255, 255, 0.16);
            backdrop-filter: blur(10px);
            box-shadow: 0 16px 40px rgba(0, 0, 0, 0.28);
        }

        h1 {
            margin-top: 0;
            color: #ffae52;
            font-size: 1.5rem;
        }

        p {
            line-height: 1.6;
            color: #ececff;
        }

        h2 {
            margin-top: 24px;
            font-size: 1rem;
            color: #bbabb8;
        }

        pre {
            background: rgba(0, 0, 0, 0.22);
            border: 1px solid rgba(255, 255, 255, 0.12);
            color: #f9fafc;
            border-radius: 10px;
            padding: 12px;
            overflow-x: auto;
            white-space: pre-wrap;
            word-wrap: break-word;
        }

        .back {
            display: inline-block;
            margin-top: 8px;
            color: #fff;
            text-decoration: none;
            border: 1px solid rgba(255, 255, 255, 0.25);
            border-radius: 999px;
            padding: 10px 16px;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>Что-то пошло не так</h1>
        <p><%= message %></p>
        <% if (error && error.stack) { %>
            <h2>Детали ошибки:</h2>
            <pre><%= error.stack %></pre>
        <% } %>
        <a class="back" href="javascript:history.back()">Вернуться назад</a>
    </div>
</body>
</html>
